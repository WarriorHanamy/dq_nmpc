"""ROS2 adapter layer (optional). Bridges SHM <-> ROS2 topics.

For the main runtime, use dq_nmpc.orchestrator / dq_nmpc.nmpc.runner instead.
The ROS adapter runs in Docker via docker/dq_nmpc_ros2.Dockerfile.
"""
